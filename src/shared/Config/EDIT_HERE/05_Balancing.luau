--!strict

return {
	Characters = {
		-- Public players start with one civilian identity. ExtraCharacterSlot
		-- grants one additional identity without changing the public role roster.
		FreeSlots = 1,
		MaximumSlots = 6,
		MinimumAge = 16,
		MaximumAge = 100,
	},
	-- Add other small balancing overrides here using the same table path shown
	-- in Config. Do not copy whole catalogues into this file.
}
